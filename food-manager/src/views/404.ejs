<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Página no encontrada</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="flex items-center justify-center min-h-screen bg-gradient-to-br from-pink-200 via-purple-200 to-blue-200 p-5">
    <div class="bg-white bg-opacity-80 p-10 max-w-lg text-center rounded-2xl shadow-lg">
        <h1 id="error-code" class="text-9xl font-bold text-pink-500 drop-shadow-lg">404</h1>
        <h2 class="text-2xl font-semibold text-gray-800 mb-4">¡Ups! Parece que te has perdido en el ciberespacio</h2>
        <p class="text-lg text-gray-600 mb-6">No te preocupes, incluso los mejores exploradores se pierden a veces.</p>
        <a href="/" class="inline-block px-6 py-3 bg-pink-500 text-white font-bold rounded-full transition-transform hover:bg-pink-600 hover:-translate-y-1">Volver a casa</a>
        <img src="/img/placeholder_404.webp" alt="Astronauta perdido" class="mx-auto mt-6 max-w-xs" id="astronaut">
    </div>

    <script>
        // Animación simple para el código de error
        const errorCode = document.getElementById('error-code');
        errorCode.style.opacity = '0';
        errorCode.style.transform = 'scale(0.5)';
        setTimeout(() => {
            errorCode.style.transition = 'opacity 0.5s, transform 0.5s';
            errorCode.style.opacity = '1';
            errorCode.style.transform = 'scale(1)';
        }, 100);

        // Animación para el astronauta
        const astronaut = document.getElementById('astronaut');
        astronaut.style.animation = 'float 3s ease-in-out infinite';

        // Definir la animación flotante en el astronauta
        document.styleSheets[0].insertRule(`
            @keyframes float {
                0% { transform: translateY(0px); }
                50% { transform: translateY(-20px); }
                100% { transform: translateY(0px); }
            }
        `, 0);
    </script>
</body>
</html>
